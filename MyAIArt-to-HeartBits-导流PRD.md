# MyAIArt → HeartBits 导流方案 PRD

> 目标：将 MyAIArt（AI 图像生成工具）的用户，导流到新产品 HeartBits（AI 角色陪伴产品）。共三个入口位。

---

## 一、方案总览

| 编号 | 入口位置 | 展示范围 | 核心逻辑 |
|------|----------|----------|----------|
| 入口1 | 顶部导航栏 | 全站所有页面 | 永久入口，被动曝光，不打扰 |
| 入口2 | 工具页顶部通栏 | `/features/*` 工具详情页、OC Maker 等角色相关工具页 | 场景触发：用户正在做"角色"相关事 |
| 入口3 | 积分不足弹窗 | 用户点击生成但积分耗尽时 | 痛点触发：用户遇到阻碍时给新出路 |

**通用规则：**

- **所有入口点击后一律新标签页打开** HeartBits（`https://www.heartbits.ai/`），不打断用户在 MyAIArt 的当前流程
- 所有上线文案均为英文（MyAIArt 无中文用户群，美国流量占 30%）

---

## 二、入口1：顶部导航栏入口

### 2.1 位置

站点顶部导航栏右侧区域，在现有导航项（Text to Image / Image to Image / AI Video Generator / OC Maker / Trending Filters / Community / Pricing）之后、语言切换和登录按钮之前。

### 2.2 视觉样式

- HeartBits 心形 logo（使用 HeartBits 品牌色，紫→粉渐变）+ 文字 `HeartBits AI`
- 和其他导航项保持一致的字体、字号、hover 态
- 不做特殊高亮，不闪、不跳、不加小红点

### 2.3 交互

- 点击：新标签页打开 `https://www.heartbits.ai/`
- hover：文字变色（和其他导航项一致）

### 2.4 展示规则

- **所有用户、所有页面、所有登录态都显示**
- 不需要关闭按钮
- 无频次限制（它就是导航的一部分，和 Pricing / Community 一样）

---

## 三、入口2：工具页顶部通栏（Banner）

### 3.1 位置

在顶部导航栏下方、面包屑/页面主内容上方，一条通栏横幅。高度约 40–48px。

### 3.2 展示范围

**展示的页面：**

- `/features/*` 工具详情页（即各个滤镜/角色生成器落地页）
- `/zh/oc-maker/`（OC Maker 角色生成器页）

**不展示的页面：**

- 首页（`/`）
- Pricing、Community、Login 等非工具页
- Text to Image / Image to Image 通用创作台（非角色相关工具）

判断标准：页面核心产出是"角色/人物"时展示，风景/物体/通用图像类工具不展示。

### 3.3 视觉样式

- 背景：深色（`#1a1a1a` 或跟随站点深色主题），上下有细微边框线
- 内容水平居中或左对齐：**心形 logo + 一句话文案 + 右箭头（→）**
- 整条通栏可点击，鼠标 hover 时背景微亮

### 3.4 文案（固定一条，不轮播）

```
💜 Characters that talk back — HeartBits AI  →
```

> 文案原则：一个差异化钩子（Characters that talk back）+ 产品名，用户扫一眼 0.3 秒能读完。

### 3.5 交互

- 点击通栏任意位置：新标签页打开 `https://www.heartbits.ai/`
- **不设关闭按钮**：条高度低、不挡内容，关闭会引入"关一次是否永久不再显示"的状态管理问题，无必要
- 无频次限制，不轮播，不自动消失

---

## 四、入口3：积分不足弹窗卡片

### 4.1 触发时机

用户已用完当日/账户积分，点击「生成」按钮时弹出 "Insufficient Credits?"（积分不足）模态窗口。这是现有弹窗，本次在底部追加第 4 个选项。

### 4.2 位置

弹窗内现有三个卡片（Daily Check-in / Join Community / Buy Credits）**之后**，作为第 4 个选项卡片。

### 4.3 视觉样式

- 卡片高度、圆角、左右内边距与前三个卡片保持一致
- 图标用心形 logo（HeartBits 品牌色）
- 背景用轻微区分于前三个卡片的色调（例如比其他卡片略浅/略暖的底色），让用户感知到这是"另一条路"而非又一个赚积分的方法
- 按钮样式与 `Buy Now` 区分开，用 HeartBits 品牌色（紫→粉渐变）而非橙→粉渐变

### 4.4 文案

**标题：**

```
HeartBits AI
```

**正文（一句话）：**

```
Out of credits? Meet HeartBits — no credits needed. Design a character that chats with you.
```

**按钮：**

```
Get Started For Free
```

### 4.5 交互

- 点击卡片任意区域/按钮：新标签页打开 `https://www.heartbits.ai/`
- 弹窗本身的关闭、其他卡片（签到 / Discord / 购买）的原有逻辑不变
- **不拦截弹窗关闭**：用户点右上角 × 仍可正常关闭弹窗，回到原页面

---

## 五、不在本期范围内

以下方案**本次不做**，避免范围膨胀：

- 生成结果图旁常驻入口（生成结果不一定是人物，容易出戏）
- 出图后全屏插屏（对已付费/高活跃用户打扰大）
- 生成中等待卡片视频位（开发成本高，等待时长不稳定）
- 登录/注册流程里插入引导（影响核心转化漏斗，风险大）
- 中文文案（MyAIArt 无中文流量，只做英文）

---

## 六、验收标准

1. 导航栏所有页面可见 HeartBits AI 入口，点击新标签页打开
2. 工具详情页（`/features/*`、OC Maker）导航栏下方有一条通栏，文案为 `Characters that talk back — HeartBits AI`，点击新标签页打开
3. 非工具页（首页、Pricing、Community、通用创作台）不展示通栏
4. 积分不足弹窗在原有三个选项下方出现第 4 个 HeartBits 卡片，文案和按钮如上
5. 所有链接均为新标签页打开，不打断用户在 MyAIArt 的当前操作
